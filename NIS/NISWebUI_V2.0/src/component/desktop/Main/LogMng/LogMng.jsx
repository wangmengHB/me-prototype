import React from 'react';
import PropTypes from 'prop-types';

export default class LogMng extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div> hello LogMng. </div>
        );
    }
}

LogMng.propTypes = {

};