import React from 'react';
import PropTypes from 'prop-types';

export default class AssetMng extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
          <div> hello asset. </div>
        );
    }
}

AssetMng.propTypes = {
    
};